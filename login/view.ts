const logInForm: HTMLFormElement | null =
  document.querySelector(".log_in_form");
const customerLink: HTMLAnchorElement | null = document.querySelector(
  "#link_to_customer_page"
);
const adminLink: HTMLAnchorElement | null = document.querySelector(
  "#link_to_admin_page"
);
